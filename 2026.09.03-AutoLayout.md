# Auto Layout

### 1\. Frame이 있는데 Auto Layout이 필요한 이유
*   뷰의 위치와 크기를 frame으로 직접 정할 수 있다.
*   화면이 딱 한 종류라면 frame으로 위치를 정하는 것이 문제 없지만, 현실은
    *   iPhone SE는 폭이 375, iPhone 15 Pro Max는 430.
    *   세로로 들었을 때랑 가로로 눕혔을 때 크기가 다르다.
    *   글자 크기를 키우는 접근성 설정을 켜면 텍스트가 커진다.
*   "오른쪽 끝에서 20 떨어진 버튼" 을 frame으로 만들려면 기기마다 x값을 다르게 계산해야 한다.
    *   375 화면이라면, x = 375 - 버튼폭-20
*   이런 것들을 해결하기 위해 나온 것이 Auto Layout 이다.
    *   Auto Layout은 "숫자를 못 박는" 대신 "관계(규칙)를 선언하는" 방식이다.
*   "어디에 놓을지(결과값)"를 내가 계산하는 게 아니라, "어떤 규칙을 지켜야 하는지"를 선언하면 결과값은 시스템이 해결한다.

### 2\. 제약(Cosntraint)
*   규칙 하나하나를 제약(Constraint)이라고 부른다.
*   제약 하나는 뷰 두 개 사이의 관계를 나타내는 수학 등식이다.

```plain
뷰1.속성 = 뷰2.속성 x 곱수(multiplier) + 상수(constant)

ex) 버튼의 오른쪽 끝을 화면 오른쪽 끝에서 20만큼 안쪽에
버튼.right = 화면.right x 1.0 + (-20)
```

*   버튼.right, 화면.right → 관계 맺는 두 속성(attribute)

### 3\. translatesAutoresizingMaskIntoConstraints
*   옛날 방식(frame 지정 방식)과 새 방식(Auto Layout)이 충돌하는 걸 막는 스위치
    *   뷰를 코드로 만들면 이 값이 기본 true이다.
    *   true면 시스템이 뷰의 옛날 frame 정보를 자동으로 제약으로 바꿔서 추가한다.
    *   여기서 직접 제약을 또 걸면? 자동 생성된 제약 vs 직접 생성한 제약이 충돌한다.
*   따라서 코드로 제약을 걸 땐 항상 이걸 false로 꺼야 한다.

```swift
let button = UIButton()
button.translatesAutoresizingMaskIntoConstraints = false
view.addSubview(button)

NSLayoutConstraint.activate([
  button.trailingAnchor.constraint(equalTo: view.trailingAnchor, constant: -20)
  button.topAnchor.constraint(equalTo: view.topAnchor, constant: 40)
])
```

### 4\. Anchor
*   위 코드에서 나온 .trailingAnchor.constraint(equalTo: ...) 를 **Anchor(앵커)** 라고 한다.
*   모든 뷰는 자기 가장자리마다 앵커를 가지고 있다.
    *   ex) view.topAnchor, button.leadingAnchor 등
*   "이 앵커를 저 앵커에 붙여라"라고 연결하면 제약이 만들어진다.

```swift
button.topAnchor.constraint(equalTo: view.topAnchor, constraint: 40)
// 버튼의 top을 view의 top에서 40 아래로
```

*   만든 제약은 활성화(activate) 해야 실제로 작동한다.

```swift
NSLayoutConstraint.activate([
  button.leadingAnchor.constraint(equalTo: view.leadingAnchor, constant: 20),
  button.trailingAnchor.constraint(equalTo: view.trailingAnchor, constant -20),
  button.topAnchor.constraint(equalTo: view.topAnchor, constant: 40),
  button.heightAnchor.constraint(equalToConstant: 30)
])
```

*   constraint를 변수에 담아두면 나중에 값을 바꿔 애니메이션으로 사용할 수 있다.

```swift
heightConstraint = button.heightAnchor.constraint(equalToConstraint: 30)
heightConstraint.isActive = true
// 나중에
heightConstraint.constant = 100
UIView.animate(withDuration: 0.3) { view.layoutIfNeeded() }
```

### 5\. Safe Area
*   아이폰은 위에 노치, 아래에 홈 인디케이터가 있다.
*   여기에 콘텐츠가 깔리면 가려지는데, 이를 위해 안전하게 콘텐츠를 놓을 수 있는 영역인 Safe Area가 있다.
*   view.topAnchor에 붙이면 노치에 가려질 수 있으니, 보통은 view.safeAreaLayoutGuide에 붙인다.

```swift
button.topAnchor.constraint(
  equalTo: view.safeAreaLayoutGuide.topAnchor, // 노치 아래에서 시작
  constant: 20
)
```

*   여기서 나온 layoutGuide는 실제 뷰는 아니지만 "제약을 걸 수 있는 보이지 않는 영역" 이다.
    *   Safe Area도 그 중 하나고, 좌우 여백을 자동으로 잡아주는 layoutMarginGuide도 있다.

### 6\. Priority
*   모든 제약은 1~1000 사이의 우선순위 숫자를 가진다.
    *   1000 = Required(필수) - 반드시 지켜야 함. 기본값
    *   1000 미만 = Optional(선택) - 가능하면 지키되, 충돌하면 양보 가능
*   언제 이걸 쓸까?
    *   "가능하면 이렇게, 안 되면 저렇게"를 표현할 때 사용한다.
    *   ex) "이 뷰의 폭은 되도록 300이면 좋겠지만, 화면이 좁으면 줄어들어도 돼"를 표현하려면 width 제약을 필수(1000)가 아니라 낮은 우선순위로 한다.
    ```swift
    let widthConstraint = box.widthAnchor.constraint(equalToConstraint: 300)
    widthConstraint.priority = .defaultHight
    widhtConstraint.isActive = true
    ```
    *   충돌 디버깅과의 연결 - 둘 다 필수(1000)라서 충돌이 발생하는 경우, 하나의 prioriy를 낮추는 방법 활용

### 7\. 콘텐츠 크기 우선순위 - Hugging과 Compression
*   배경, 레이블이나 버튼 같은 뷰는 자기가 원하는 크기가 있고, "내용에 딱 맞는 자연스러운 크기"를 intrinsic content size(내재적 콘텐츠 크기) 라고 한다.
*   레이블, 버튼, 이미지뷰 등은 이 값을 스스로 알기 때문에 width/height 제약을 안 줘도 크기가 정해지는 것.
*   그치만 공간이 애매할 때는 문제가 생김

```plain
1. 공간이 남을 때 - 누가 늘어날까? → Hugging
레이블 두 개를 가로로 놨는데 화면이 넓어서 공간이 남는 경우, 둘 중 어떤게 늘어나서 빈 공간을 차지할까?
 - Content Hugging Priority = "자기 콘텐츠 크기를 껴안고(hug) 늘어나기 싫어하는 정도"
 - 이 값이 높은 뷰는 "난 딱 내 크기만 유지할래, 늘어나기 싫어"
 - 이 값이 낮은 뷰가 양보해서 늘어남
즉 Hugging이 낮은 쪽이 늘어난다.
```

  

```markdown
2. 공간이 부족할 때 - 누가 찌그러질까? -> Compression Resistance (압축 저항)
이번엔 화면이 좁아서 두 레이블이 다 못 들어가는 경우, 하나는 글자가 잘리거나 줄어들어야 한다.
 - Content Compression Resistance Priority = "찌그러짐에 저항하는(resist) 정도"
 - 이 값이 높은 뷰는 "난 절대 안 줄어들어, 내 글자 다 보여줄 거야"
 - 이 값이 낮은 뷰가 찌그러짐 (글자가 ... 로 잘림)
즉 Compression Resistance가 낮은 쪽이 찌그러진다.
```

```plain
    // 제목은 절대 안 줄어들게 -> 압축 저항을 높임
    titleLabel.setContentCompressionResistancePriority(.required, for: .horizontal)
    // 값은 필요하면 줄어들게 -> 압축 저항을 낮춤 (기본값이 이미 낮아서 보통 생략)
    valueLabel.setContentCompressionResistancePriority(.defaultLow, for: .horizontal)
```

### 8\. Stack View
*   세로로 5개의 뷰를 균등하게 쌓으려면 많은 제약이 필요하지만, 이걸 자동으로 관리해주는게 UIStackView 이다.
*   스택 뷰에 자식 뷰들을 넣으면, 규칙에 필요한 제약을 스택 뷰가 알아서 만들어준다.

```swift
let stack = UIStackView(arrangedSubView: [imageView, titleLable, subtitleLabel])
stack.axis = .vertical          // 세로로 쌓기 (.horizontal이면 가로)
stack.spacing = 8               // 사이 간격
stack.alignment = .center       // 가운데 정렬
stack.distribution = .fill      // 공간 꽉 채우기
```

*   스택 뷰는 내부적으로 Hugging/Compression을 이용해서 누구를 늘리고 줄일지 결정한다.

### 9\. 제약이 frame으로 바뀌는 순간은?
*   실제로 반영되는 지점은 layoutSubviews 이다.
*   제약(선언) → 레이아웃 엔진이 방정식을 풂 → frame(결과값) 계산 → layoutSubviews에서 적용
