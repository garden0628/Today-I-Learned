# ViewController 생명주기

### 1\. ViewController가 필요한 이유
*   UIViewController는 화면 하나를 책임지는 관리자다.
*   View들을 만들어 조립하고, 데이터를 View에 채우고, 사용자 입력에 반응하고, 다른 화면으로의 전환을 담당한다.
*   ViewController는 View가 아니므로 화면에 보이지 않는다. 대신 View라는 프로퍼티를 하나 가지고 있고, 그게 이 컨트롤러가 관리하는 화면 전체의 최상위 view (root view) 이다.

```swift
class LoginViewController: UIViewController {
  // self.view <- 이 controller가 관리하는 최상위 뷰
}
```

*   Controller가 관리하는 퍋의 나무 전체를 뷰 계층(view hierarchy) 이라고 한다.

### 2\. 생명주기
*   ViewController는 태어나서, 화면에 나타났다가, 사라졌다가, 결국 메모리에서 없어진다.
*   이 과정에서 **"지금 이 단계에 진입했습니다"** 라고 알려주는 메서드들이 있는데, 이를 생명주기(lifecycle) 메서드라고 한다.
*   우리가 이 메서드들을 직접 호출하는 게 아니라, 시스템이 적절한 시점에 불러주는 것이다. 우리는 이 시점에 어떤 것을 할지를 정해서 오버라이드 하여 넣는다.
*   왜 이런 구조일까? → 시점마다 **할 수 있는 일과 없는 일이 다르기 때문**이다.

### 3\. 뷰의 생성 시점

```swift
let vc = LoginViewController() // controller 객체는 만들어졌다
// 하지만 이 시점에 vc.view는 아직 존재하지 않는다.
```

*   ViewController는 만들어져도 view는 즉시 만들어지지 않는다. 화면이 실제로 필요해지는 순간까지 미뤄둔다. → 메모리를 아끼기 위해서

```swift
let vc = DetailViewController()
vc.view.backgroundColor = .white // 이 줄에서 view가 강제로 로드된다.
navigationController?.pushViewController(vc, animated: true)
```

*   위는 view를 미리 만들어버리는 경우이다. 그래서 화면 전환 전에 데이터를 넘길 땐 view를 건드리지 말고 프로퍼티로 넘기는게 원칙이다.

```swift
let vc = DetailViewController()
vc.productID = 42 // view를 건드리지 않음
navigationController?.pushViewController(vc, animated: true)
```

### 4\. 생명주기 전체 순서
*   \[생성\] : init → loadView(view를 실제로 만드는 곳) → viewDidLoad(view가 만들어진 직후, 딱 한번)
*   \[나타남\] : viewWillAppear(화면에 나타나기 직전) → viewIsAppearing(나타나는 중, iOS 17+) → viewWillLayoutSubviews / viewDidLayoutSubviews (배치, 여러 번) → viewDidAppear(화면에 완전히 나타난 후)
*   \[사용 중\]
*   \[사라짐\] : viewWillDisappear(사라지기 직전) → viewDidDisappear(완전히 사라진 후)
*   \[소멸\] : deinit(메모리에서 해제)

### 5\. loadView() - view를 만드는 곳
*   이 메서드의 유일한 임무는 self.view에 view를 하나 만들어 넣는 것이다.
*   기본 구현은 스토리보드/xib가 있으면 거기서 view를 불러오고, 없으면 빈 UIView를 하나 만든다.
*   따라서 보통 수정할 일이 없지만, 코드로만 UI를 짜는 경우, 최상위 뷰를 custom view로 교체하고 싶을 때만 오버라이드한다.

```swift
override func loadView() {
  view = LoginRootView() // 최상위 뷰를 내가 만든 커스텀 뷰로 교체
}
```

*   주의할 점 1 : 오버라이드할 땐 super.loadView() 를 호출하면 안된다.
    *   기본 뷰를 만든 뒤 또 만들면 낭비이기 때문
*   주의할 점 2 : loadView()안에서 self.view를 읽으면 안된다.
    *   아직 없는 뷰를 읽으려 하면 → 시스템이 "어? 뷰 필요하네" → loadView 호출 → 또 self.view 읽음 → 무한 반복으로 앱 크래시

### 6\. viewDidLoad() - 딱 한번, 초기 세팅의 자리
*   view가 만들어진 직후에 불리며, **뷰의 일생에서 딱 한 번만** 샐행된다.
*   viewDidLoad()에는 "딱 한번만 하면 되는 초기 설정"을 넣는다.

```swift
override func viewDidLoad() {
  super.viewDidLoad()

  view.backgroundColor = .systemBackground
  view.addSubview(tableView)
  setupConstraints()
  tableView.dataSource = self
  loginButton.addTarget(self, action: #selector(didTapLogin), for: .touchUpInside)
  titelLabel.text = "로그인"
}
```

*   viewDidLoad()에 넣으면 안 되는 것 - **frame에 의존하는 코드**
    *   viewDidLoad 시점에 뷰는 존재하지만, 레이아웃 사이클이 아직 안 돌았기 때문에 크기가 아직 확정되지 않는다.
    ```swift
    // 따라서 아래와 같은 코드는 잘못된 값을 계산된다.
    override func viewDidLoad() {
      super.viewDidLoad()
      profileImageView.layer.cornerRadius = profileImageView.frame.width / 2
      // frame.width가 아직 제대로 안 정해져서 cornerRadius가 안 만들어진다.
    }
    ```
    *   frame(위치, 크기)에 의존하는 계산은 레이아웃이 끝난 뒤에 해야 한다. → viewDidLayoutSubviews

### 7\. viewWillAppear() - 나타날 때마다 매번
*   화면에 나타나기 직전에 호출되며, 나타날 때마다 매번 불린다.
*   viewWillAppear()에는 "화면에 보일 때마다 갱신해야 하는 것" 을 넣는다.

```swift
override func viewWillAppear(_ animated: Bool) {
  super.viewWillAppear(animated)

  refreshData()
  navigationController?.setNavigationBarHidden(true, animated: animated)
  startObservingKeyboard()
}
```

*   viewDidLoad와 viewWillAppear의 차이는?
    *   호출 횟수가 다르면 갱신이 필요한 데이터는 viewWillAppear에 둬야 한다.

### 8\. viewIsAppearing() - iOS 17에 추가된 메소드
*   viewWillAppear는 너무 일러서 frame이 확정 안됐고, viewDidAppear는 너무 늦어서 화면이 이미 보인 뒤라 여기서 UI를 바꾸면 사용자 눈에 깜빡임이 보였다.
*   viewIsAppearing은 그 사이를 메운다.
*   뷰가 계층에 붙었고 frame도 확정됐지만, 아직 화면 전환은 진행 중인 시점이다.
*   즉, "크기를 알아야 하는데, 사용자에게 보이기 전에 끝내고 싶은 일" 을 처리하기 위한 메소드

```swift
override func viewIsAppearing(_ animated: Bool) {
  super.viewIsAppearing(animated)
  tableView.scrollToRow(at: indexPath, at: .middle, animated: false)
}
```

### 9\. viewWillLayoutSubviews / viewDidLayoutSubviews - 배치 전후
*   viewWillLayoutSubviews - 최상위 뷰가 자식들을 배치하기 직전
*   viewDidLayoutSubviews - 배치가 끝난 직후 (frame이 확정된 시점)

```swift
// 아까 미뤄둔 문제가 여기서 적용될 수 있다
override func viewDidLoad() {
  super.viewDidLoad()
  profileImageView.layer.cornerRadius = profileImageView.frame.width / 2
  // frame.width가 아직 제대로 안 정해져서 cornerRadius가 안 만들어진다.
}
```

*   layoutSubviews와 마찬가지로 여러 번 불린다.
    *   무거운 작업(네트워크, 큰 계산)을 넣으면 안된다.
    *   여기서 레이아웃을 또 바꾸면 다시 viewDidLayoutSubviews가 불려서 무한 루프에 빠질 수 있다.

### 10\. viewDidAppear() - 완전히 나타난 후
*   화면 전환 애니메이션까지 다 끝나고, 사용자에게 완전히 보이는 상태
*   viewDidAppear()에서는 "화면이 실제로 보인 뒤에 시작해야 하는 것" 을 넣는다.

```swift
override func viewDidAppear(_ animated: Bool) {
  super.viewDidAppear(animated)

  startAnimation()
  videoPlayer.play()
  analytics.logScreenView("Login")
  searchBar.becomeFirstResponder()
}
```

*   애니메이션 시작, 영상 재생, 화면 조회 로그, 키보드 올리기 등을 viewDidAppear에 두는 이유
    *   화면이 아직 안 보이는데 애니메이션이 시작되면 사용자가 못 보고 놓친다.
    *   화면 조회 로그도 실제로 보인 시점에 찍어야 정확하다.
    *   키보드를 viewWillAppear 에서 올리면 전환 애니메이션과 충돌할 수 있다.

### 11\. viewWillDisappear / viewDidDisappear - 사라질 때
*   화면이 가려지거나 없어질 때 불린다.

```swift
override func viewWillDisappear(_ animated: Bool) {
  super.viewWillDisappear(animated)
  saveDraft()                  // 작성 중이던 내용 저장
  view.endEditing(true)        // 키보드 내리기
}

override func viewDidDisappear(_ animated: Bool) {
  super.viewDidDisappear(animated)
  videoPlayer.pause()          // 영상 정지
  timer?.invalidate()          // 타이머 정리
  NotificationCenter.default.removeObserver(self)    // 구독 해제
}
```

*   viewWillAppear에서 시작한 건 viewWillDisappear(또는 viewDidDisappear)에서 정리한다.

### 12\. deinit - 메모리에서 해제될 때
*   뷰 컨트롤러 객체 자체가 메모리에서 없어질 때 부른다.
*   사라지는 것(viewDidDisappear)과 해제되는 것(deinit)은 다른다.

```swift
deinit {
  print("LoginViewController 해제됨")
}
```

*   개발 중 deinit에 로그를 찍어두면 메모리 누수를 쉽게 발견할 수 있다.
    *   화면을 닫았는데 이 로그가 안 찍힌다면, 어딘가에서 이 컨트롤러를 붙잡고 안 놔주고 있다는 뜻

### 13\. 실제 상황별 호출 순서
1.     1. A → B로 Push (네비게이션)

```bash
A.viewWillDisappear
B.viewWillAppear
B.viewIsAppearing
B.viewDidLayoutSubviews
A.viewDidDisappear
B.viewDidAppear
```

2.     1. B에서 뒤로 가기 (pop)

```plain
B.viewWillDisappear
A.viewWillAppear      <- A의 viewDidLoad는 불리지 않음
A.viewDidAppear
B.viewDidDisappear
B.deinit              <- B는 메모리에서 해제
```

3.     1. 전체화면 모달 띄우기 - A를 완전히 덮으므로 A.viewWillDisappear → A.viewDidDisappear
    2. 시트(sheet) 모달 띄우기
        *   A를 완전히 가리지 않아, A.viewWillDisappear가 불리지 않는다.
        *   모달을 닫고 돌아오면 데이터가 갱신되지 않는다 → 애초에 사라진 적이 없으니 viewWillAppear가 불리지 않는다.
            *   해결책은 delegate나 클로저로 모달이 닫힐 때 직접 알려주는 것
4.     1. 앱을 백그라운드로 보내면?
        *   생명주기 메서드가 안 불린다.
        *   화면은 여전히 "나타나 있는" 상태이기 때문
        *   앱 전환은 별개의 앱 생명주기(Scene) 영역이다.
