# Frame & Bounds

View 들의 위치와 크기를 조절하면서 Frame과 Bounds를 많이 접하게 되었고, 이 둘의 차이점을 정리해보고자 한다.

### 1\. Frame
: SuperView의 좌표계에서 뷰의 위치와 크기를 나타내는 사각형이 frame 이라고 한다.

### 2\. Bounds
: View의 자체 좌표계에서 위치와 크기를 나타내는 사각형이 bounds 이다.

즉, Frame은 선택한 뷰(SuperView, SubView, SubView의 Subview)가 부모(SuperView)의 좌표계 기준으로 어디에, 얼마나 크기 위치하는지를 나타내고, Bounds는 현재 선택된 View 자신의 좌표계와 크기를 나타낸다.

### 3\. Questions
그렇다면, 초기 최상위 view에서는 frame과 bounds가 같은 걸가??
→ UIWindow나 rootViewController의 view처럼 최상위에 있고, origin이 (0,0)에 위치하는 경우엔:
*       *   frame.origin = (0, 0) - 부모 좌표계에서 원점에 있으므로
    *   bounds.origin = (0, 0) - 초기 자신의 자표계 원점은 기본값이 (0, 0) 이므로
    *   size는 둘 다 화면 크기와 동일
→ 결과적으로 frame == bounds 로 보인다.

그렇다면, UIScrollView에서는 어떻게 되는 것일까?
→ Content를 스크롤하게 되면 bounds.origin이 변경된다 (ex. (0, 500))
→ 하지만 frame은 그대로이므로 frame과 bounds는 달라지게 된다.

### 4\. Examples
추가적인 예로 아래와 같은 상황이 있다고 생각해보자

```yaml
SuperView frame : (0.0, 0.0, 393.0, 852.0)
SuperView bounds : (0.0, 0.0, 393.0, 852.0)
SubView frame : (50.0, 100,0, 200.0, 200.0)
SubView bounds : (0, 0, 200.0, 200.0)
```

→ 이때의 경우, subView의 origin은 SuperView의 좌표계에서 봤을 때 (50.0, 100.0)의 위치에서 시작되는 사각형인 것이다.

그렇다면 만약, subView의 origin을 (0.0, 0.0)으로 바꿔주게 된다면 어떻게 될까?
→ SubView는 superView의 원점부터 시작하게 되는 뷰가 된다.

SubView의 origin을 바꾸지 않고 subView가 superView의 원점에서 시작하게 하는 방법은 없을까?
→ SuperView의 bounds를 수정해주면 된다!

```plain
superView.bounds.origin.x = 50.0
superView.bounds.origin.y = 100.0
```

→ 위처럼 수정해주면 superView 자체의 자표가 원점이 (50.0, 100.0)으로 이동하기 때문에, subView가 superView의 원점에서 시작되게 된다.
