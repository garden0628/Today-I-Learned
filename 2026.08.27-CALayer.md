# CALayer

*   UIView는 CALayer를 감싸는 wrapper일 뿐이고, 실제로 화면에 그려지는 건 CALayer이다.

### 1\. CALayer
*   실제 시각적 내용(content)를 담고, 화면에 그려지는(렌더링) 일을 담당한다.
*   backgroundColor, cornerRadius, frame 같은 시각 정보가 사실은 전부 layer에 저장된다.
    *   \=> UIView는 화면을 직접 그리지 않는다. 그림 그리는 일은 CALayer라는 다른 객체에게 맡긴다.

```plain
// 아래처름 코드를 작성하였을 때,
box.backgroundColor = .red

// 실제 내부에서는 box.layer.background = .red 가 되는 것이다.
```

### 2\. UIView에서 하는일은?
*   layer를 감싸면서, layer가 못하는 일을 한다
    *   터치 이벤트 처리(UIView는 UIResponder를 상속 받음)
    *   AutoLayout 참여

### 3\. CALayer와 UIView를 나눈 이유는?
*   최종적인 이유는 "책임 분리" 때문이다.
*   화면에 그리는 일과 사용자 입력(터치)을 처리하는 일은 성격이 완전히 다른 작업이다. Apple은 이 둘을 다른 프레임워크로 나누었다.
    *   그리기/애니메이션 → Core Animation 프레임워크
    *   터치/레이아웃 → UIKit 프레임워크
*   이렇게 나눔으로서 생기는 이점
    *   강력한 애니메이션 : iOS 애니메이션은 전부 layer 위에서 일어난다. 렌더링 담당인 layer가 애니메이션까지 담당하여 자연스러워진다.
    *   성능 최적화 지점 : 성능 문제를 다룰 지점이 명확해진다. "화면 스크롤이 버벅인다" 같은 성능 이슈는 전부 layer 레벨에서 이루어진다. 문제의 위치가 layer로 특정되어 성능 최적화 하기에 용이하다.
