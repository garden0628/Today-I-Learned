# Event Handler

화면을 손가락으로 눌렀을 때, 시스템은 두 가지를 순서대로 알아내야 한다.
1. 어느 뷰를 누른건지 - 화면엔 뷰가 수십 개 겹쳐 있는데, 그중 어떤 뷰가 이 터치의 주인인지 찾아야 한다. → 이 과정이 Hit Testing
2. 그 뷰가 터치를 처리 못 하면, 누구에게 넘길지 - 눌린 뷰가 정작 이 터치를 어떻게 다룰지 모를 수 있으므로 누군가에게 책임을 넘겨야 한다. → 이 과정이 Responder Chain

### UIResponder
*   이벤트를 받고 처리할 수 있는 객체들은 전부 UIResponder라는 클래스를 상속받는다.
*   UIResponder를 상속받는 객체 : UIView, UIViewController, UIApplication, UIWindow
*   UIView는 CALayer에게 그리기를 맡기고, 자신은 터치 처리와 Auto Layout을 담당한다고 했는데, 이때 터치 처리 능력이 UIResponder를 상속 받아서 생긴 것이다. CALayer는 UIResponder가 아니라 터치를 받을 수 없다. (순수 layer가 버튼을 못 만드는지도 같은 이유)
*   UIResponder는 터치를 다루는 메서드들을 가지고 있다.

```swift
func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) // 손가락이 닿음
func touchesMoved(_ touches: Set<UITouch>, with event: UIEvent?) // 움직임
func touchesEnded(_ touches: Set<UITouch>, with event: UIEvent?) // 손가락을 뗌
func touchesCancelled(_ touches: Set<UITouch>, with event: UIEvent?) // 취소됨 (전화 등)
```

### Hit Testing
*   시스템은 뷰 게층 나무를 위에서 아래로 훑으며 재귀적으로 눌린 뷰를 찾는다.
*   핵심 원리 : 부모에게 먼저 묻고, 부모가 자식들에게 다시 묻는다.

```swift
func hitTest(_ point: CGPoint, with event: UIEvent?) -> UIView?
```

*   위 메서드는 내부에서 point(inside:with:) 라는 메서드를 써서 해당 좌표가 본인의 영역안에 있는 판단한다.
    *   point(inside:with:) - "이 점이 내 사각형 안에 있나?"
    *   hitTest(\_:with:) - 위 판단을 이용해 나무 전체에서 최종 뷰를 찾아 반환
*   터치를 못 받는 조건
*   어떤 뷰는 hit test에서 아예 건너뛰어진다. 다음 중 하나라도 해당하면 그 뷰(와 그 자식들)는 터치를 받을 수 없다.
    *   isUserInteractionEnabled = false - 상호작용 끔. UIImageView는 이게 기본 false라서 위에 제스처를 달아도 적용이 안되는 경우가 많다.
    *   isHidden = true - 숨겨진 뷰
    *   alpha <= 0.01 - 거의 투명한 뷰
    *   부모 영역 밖으로 삐져나간 부분 - clipsToBounds와 무관하게, 부모의 point(insde:)를 통과 못하면 그 자식의 삐져나온 부분은 터치가 안된다.
*   hitTest 커스텀
*   hitTest를 오버라이드하면 터치 판정을 마음대로 바꿀 수 있다.

```swift
override func hitTest(_ point: CGPoint, with event: UIEvent?) -> UIView? {
  // 실제 버튼보다 상하좌우 20씩 넓은 영역을 터치 가능하게
  let expanded = bounds.insetBy(dx: -20, dy: -20)
  if expanded.contains(point) {
    return self
  }
  return super.hitTest(point, with: event)
}
```

### Responder Chain
*   눌린 뷰에서 시작해서, 처리할 수 있는 누군가를 만날 떄까지 위로 사슬을 타고 올라가는 것이, responder chain 이다.
*   사슬의 순서

```plain
눌린 뷰 (hit-test view) -> 그 뷰의 부모 뷰 (superview) -> 그 부모의 부모 뷰 ... -> 그 뷰들을 관리하는 뷰 컨트롤러 (UIViewController) -> 윈도우 (UIWindow) -> 앱 객체 (UIApplication) -> 앱 델리케이트 (UIApplicationDelegate) -> 아무도 처리 안 하면 -> 이벤트 버려짐
```

*   nextResponder - 각 responder는 next라는 프로퍼티로 자신의 위 responder를 알고 있다.
*   Responder chain 덕분에 여러 자식 뷰의 터치를 부모나 뷰 컨트롤러가 한곳에서 처리할 수 있다. 자식마다 일일이 처리 코드를 안 넣어도, 공통 뷰 컨트롤러에서 받아 처리하면 된다.

### Target - Action

```swift
button.addTarget(self, action: #selector(didTapButton), for: .touchUpInside)

@objc func didTapButton() {
  print("버튼 눌림!)
}
```

*   target: self - 누구에게 알릴 것인가 (보통 뷰 컨트롤러 자신)
*   action: #selector(didTapButton) - 어떤 메서드를 부를 것인가
*   for: .touchUpInside - 어떤 이벤트일 때 (버튼 안에서 눌렀다 뗐을 때)

### Gesture Recognizer
*   Target-Action은 버튼의 단순 탭엔 좋지만, "두 손가락으로 확대", "길게 누르기", "스와이프" 같은 복잡한 동작은 안된다. 이를 위해 Gesture Recognizer가 존재한다.
*   Gesture Recognizer는 터치의 연속적인 흐름을 분석해서 동작을 알아내는 객체이다.
    *   UITapGestureRecognizer - 탭(한 번/여러 번)
    *   UILongPressGestureRecognizer - 길게 누르기
    *   UIPanGestureRecognizer - 드래그(끌기)
    *   UIPinchGestureRecognizer - 두 손가락 확대/축소
    *   UISwipeGestureRecognizer - 방향 쓸기
    *   UIRotationGestureRecognizer - 회전

```swift
let tap = UITapGestureRecognizer(target: self, action: #selector(didTap))
imageView.isUserInteractionEnabled = true
imageView.addGestureRecognizer(tap)

@obj func didTap() {
  print("이미지 탭됨")
}
```
