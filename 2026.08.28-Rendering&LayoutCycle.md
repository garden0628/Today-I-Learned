# Rendering & Layout Cycle

### 1\. App 동작방식
*   앱은 실행되면 가만히 있는 것이 아니라, 속에서 "무한 반복문"이 돌고 있다고 생각하면 된다.
*   이걸 "Run Loop(런 루프)" 라고 한다.
*   반복문은 한 바퀴 돌 때마다 아래와 유사한 일을 한다.

1\. 사용자가 화면을 터치했는가? → 처리

2\. 타이머가 울렸는가? → 처리

3\. 네트워크 응답이 왔는가? → 처리

4\. 화면에 다시 그려야 할게 있는가? → 그림

5\. 할 일이 없으면 → 잠깐 잠들었다가 다음 이벤트가 오면 깨어남

*   여기서 핵심은 4번, 런 루프가 한 바퀴 도는 끝자락에 한꺼번에 화면을 다시 그린다.

### 2\. UIKit 동작방식

```rust
box.frame.size.width = 100
box.frame.size.width = 200
box.frame.size.width = 300
```

*   코드 상으로 화면을 3번 다시 그리는건가 싶지만, UIKit은 그렇지않다.
*   일단 '이 뷰를 다시 그려야 함' 이라고 메모(표시)만 해두고, 런 루프 끝날 때 최종 상태 한 번만 그린다.
*   이것을 "invalidation(무효화) → 나중에 한 번에 처리(coalescing)" 패턴이라고 한다.
*   이러한 이유는 "성능" 때문이다.

### 3\. UIKit의 화면 갱신
1.     1. 레이아웃(Layout) 사이클
    \- 뷰들의 위치와 크기를 정하는 단계이다.
    2. 디스플레이(Display) 사이클
    \- 정해진 영역 안을 실제로 그리는 단계이다.
*   UIKit은 위 두 사이클을 별개로 관리한다.

### 4\. Layout Cycle
*   layoutSubviews() - 실제 배치 작업이 일어나는 곳 (진짜 일꾼)
    *   이 안에서 자식 뷰들의 frame이 실제로 계산 및 확정된다.
    *   이 메서드를 직접 호출하는 일은 절대 없지만, 배치 로직을 커스텀하고 싶을 때 이 메서드를 오버라이드만 한다.
*   setNeedsLayout() - "나중에 배치 다시 해줘" 라고 메모만 (예약)
    *   이 메서드는 "이 뷰 레이아웃 다시 필요함"이라고 표시만 해두고 즉시 리턴한다.
    *   실제 layoutSubviews는 런 루프 끝에 몰아서 부른다.
*   layoutIfNeeded() - "예약해둔 배치, 지금 당장 해" (강제 즉시 실행)
    *   에약(needs)이 걸려있으면 러 루프 끝을 기다리지 말고 지금 이 줄에서 즉시 layoutSubViews를 실행한다.
*   정리하자면, setNeedsLayout은 예약, layoutIfNeeded는 즉시 실행, layoutSubviews는 실제로 불려서 일하는 곳이다.

*   위 3개의 메서드가 다 필요한 이유 - 애니메이션 예시

```plain
heightConstraint.constant = 300 // 높이를 300으로 바꾸겠다고 값만 변경

UIView.animate(withDuration: 0.3) {
  self.view.layoutIfNeeded() // 이 시점의 배치를 '지금' 계산해서 부드럽게 이동
}
```

*       *   제약(Constraint) 값만 바꾸면 UIKit은 런 루프 끝에 반영하게 된다.
    *   그럼 애니메이션 블록 안에서는 아직 아무 변화가 안 잡혀서 순식간에 뷰가 변하게 된다.
    *   그래서 애니메이션 블록 안에서 layoutIfNeeded()로 "지금 당장 새 배치를 계산해" 라고 강제하면, UIKit이 그 변화를 0.3초에 걸쳐 부드럽게 그려준다.

*   그렇다면, layoutSubviews는 언제 자동으로 불리게 되는가? (여러 번 불릴 때도 있음)
    *   뷰의 크기(bounds)가 바뀔 때 (기기 회전, 부모 크기 변화 등)
    *   addSubview로 자식이 추가/제거될 때
    *   제약(constraint)이 바뀔 때
    *   스크롤할 때 (UIScrollView 계열은 스크롤 중 계속 불림)
    *   setNeedsLayout / layoutIfNeeded 를 호출한 뒤
*   layoutSubviews는 생각보다 자주, 여러 번 불린다.
*   그래서 layoutSubviews 안에 네트워크 요청이나 무거운 계산을 넣으면 스크롤할 때마다 실행돼서 앱이 버벅인다.
*   오직 배치(frame 계산)만 하는 것을 추천한다.

### 5\. Display Cycle
*   draw(\_ rect: CGRect) - 실제로 그리는 곳
    *   이 안에서 선, 곡선, 색을 직접 그린다.
    *   직접 호출하지 않고 시스템이 불러준다.
    *   커스텀 드로잉이 필요할 때만 오버라이드한다.
*   setNeedsDisplay() - "나중에 다시 그려줘" 라고 메모 (예약)
    *   표시만 하고 리턴, 실제 draw(\_:)는 런 루프 끝에 몰아서 그린다.
    *   레이아웃의 setNeedsLayout과 동일한 원리

### 6\. 전체 파이프라인
1.     1. 제약 업데이트 - Auto Layout이 제약들을 계산
    2. 레이아웃 단계 - layoutSubviews가 불려서 모든 뷰의 frame(위치, 크기) 확정
    3. 디스플레이 단계 - 다시 그려야 하는 뷰의 draw(\_:)가 불려서 각 layer의 내용(비트맵) 생성
    4. 커밋(commit) - 완성된 layer 나무 전체를 포장해서 렌더 서버로 넘김
*   렌더 서버 - 실제 GPU를 써서 픽셀을 화면에 합성(composite)하는 일은 우리 앱 프로세스가 아니라 별도의 시스템 프로세스가 담당한다.
*   앱은 "이렇게 배치해줘"라는 (layer tree)만 넘기고, 실제 픽셀 합성은 그 전문 프로세스가 GPU로 처리하는 구조이다.

### 7\. 오프스크린 렌더링 (off-screen rendering)
*   보통 GPU는 각 layer를 화면에 바로바로 합성한다.
*   그런데 어떤 효과는 GPU가 별도의 임시 버퍼에 먼저 그린 다음 나중에 합성하기도 한다.
*   이 별도의 임시 버퍼에 작업하고 합성하는 것을 **오프스크린 렌더링**이라고 한다.
*   이 오프스크린 렌더링 비용은 비싸며, 특히 스크롤처럼 매 프레임 반복되면 뚝뚝 끊기게 된다.
*   대표적인 오프스크린 렌더링 유발 요인:
    *   그림자(shadow)를 shadowPath 없이 줄 때 - GPU가 view 모양을 매번 계산해야 해서 비싸다. 이런 경우, shadowPath를 직접 지정해주면 해결됨
    *   Mask로 실제 콘텐츠(이미지 등)를 잘라낼 때 - ex. 이미지 위에 cornerRadius + masksToBounds로 둥글게 클리핑
    *   shouldRasterize = true - layer를 통째로 비트맵으로 구워서 캐싱.

### 8\. UI 갱신
*   지금까지의 모든 레이아웃, 디스플레이 작업은 반드시 메인 스레드(main thread)에서 일어나야 한다.
*   왜냐하면 UIKit은 스레드 안전(thread-safe)하지 않게 설계되어 있음.
*   여러 스레드가 동시에 같은 뷰를 건드리면 화면이 깨지거나 앱이 죽음.
*   따라서 백그라운드에서 네트워크로 데이터를 받아온 뒤 화면을 갱신할 땐 반드시 메인으로 돌아와야 함.

```javascript
URLSession.shared.dataTask(with: url) { data, ,  in
  // 여긴 백그라운드 스레드 (UI 갱신 x)
  DispatchQueue.main.async {
    self.imageView.image = downloadImage // UI 갱신은 메인에서
  }
}.resume()
```

### 9\. 정리
*   앱 속에서는 Run Loop라는 반복문이 돌고 있고, 화면 갱신은 아무때나 즉시가 아니라 이 루프 끝자락에 몰아서 일어난다.
*   UIKit은 값이 바뀌면 "다시 해야 함" 이라고 표시만 해두고 실제 작업 마지막에 한번에 처리한다(성능 때문).
*   화면 갱신은 위치, 크기를 정하는 레이아웃 사이클(setNeedsLayout 예약 → layoutIfNeeded 즉시 → layoutSubviews 실행)과 실제로 색칠하는 디스플레이 사이클(setNeedsDisplay 예약 → draw(\_:) 실행)로 나뉘고, 이후 완성된 layer 별더 렌더 서버로 커밋되어 GPU가 화면에 합성한다.
*   cornerRadius, 그림자처럼 오프스크린 렌더링을 유발하는 효과는 성능을 저하시킬 수 이쏙, 이 모든 작업은 반드시 메인 스레드에서 이뤄져야 한다.
